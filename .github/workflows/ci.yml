name: CI
run-name: CI Workflow • ${{ github.repository }} • ${{ github.event_name }} • ${{ github.ref_name }}
permissions: read-all

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'tests/**'
      - '.github/workflows/**'
  pull_request:
    branches:
      - main
      - 'release/**'
      - 'hotfix/**'
    paths:
      - 'src/**'
      - 'tests/**'

jobs:
  setup:
    name: Setup
    uses: WarehouseFinds/PSScriptModule/.github/workflows/powershell-script-module-cache-dependencies.yml@main

  unit-tests:
    name: Unit Tests
    needs: [setup]
    uses: WarehouseFinds/PSScriptModule/.github/workflows/powershell-unit-tests.yml@main
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    permissions:
      issues: write
      pull-requests: write
      checks: write
    with:
      module-list: ${{ needs.setup.outputs.module-list }}

  static-code-analysis:
    name: Static Code Analysis
    needs: [setup]
    uses: WarehouseFinds/PSScriptModule/.github/workflows/powershell-script-module-analysis-static-code.yml@main
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    permissions:
      issues: write
      pull-requests: write
      checks: write
    with:
      module-list: ${{ needs.setup.outputs.module-list }}

  code-injection:
    name: Code Injection
    needs: [setup]
    uses: WarehouseFinds/PSScriptModule/.github/workflows/powershell-script-module-analysis-code-injection.yml@main
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    permissions:
      issues: write
      pull-requests: write
      checks: write
    with:
      module-list: ${{ needs.setup.outputs.module-list }}

  semantic-code-analysis:
    name: Semantic Code Analysis
    needs: [setup]
    uses: WarehouseFinds/PSScriptModule/.github/workflows/powershell-script-module-analysis-semantic-code.yml@main
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    permissions:
      security-events: write
      actions: read
      contents: read
      packages: read

  build:
    name: Build
    needs: [setup, unit-tests, static-code-analysis, code-injection, semantic-code-analysis]
    uses: WarehouseFinds/PSScriptModule/.github/workflows/powershell-script-module-build.yml@main
    permissions:
      contents: write
    with:
      module-list: ${{ needs.setup.outputs.module-list }}
      release-type: 'Debug'
